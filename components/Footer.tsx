export default function Footer() {
  return (
    <footer className="mt-12 border-t border-base bg-surface">
      <div className="mx-auto flex max-w-6xl flex-col gap-3 px-4 py-6 text-sm text-muted md:flex-row md:items-center md:justify-between">
        <p className="font-semibold text-primary">CampusLost+Found</p>
        <p>Keeping campus items safe and findable. Stay alert, stay kind.</p>
      </div>
    </footer>
  );
}
